# Object Oriented Programming Practice


# Fraction Class in Python

A simple Python class to represent fractions.  
- Set numerator and denominator (denominator validated to avoid zero)  
- Get fraction as tuple `(numerator, denominator)`  
- Convert fraction to decimal  

## Usage
Create a `fraction` object, set numerator and denominator, then call `convertdecimal()` to get decimal value.  

